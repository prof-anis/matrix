
language: php

 

before_script:
 - composer self-update
 - composer install --prefer-source --no-interaction --dev

script: vendor/bin/phpunit

notifications:
    on_success: never
    on_failure: always